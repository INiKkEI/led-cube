# Firmware

This folder contains the embedded firmware for LED Cube.

## Version 1 firmware goals

- initialize hardware
- scan cube layers
- control LED states
- run preloaded animations
- provide serial debug output

## Planned structure

- `src/` — source files
- `include/` — headers
- `lib/` — reusable modules
- `test/` — firmware-side test code
